'use strict';

describe('AccountActionCreators', function() {
  var action;

  beforeEach(function() {
    action = require('./AccountActionCreators.js');
  });

  it('should be defined', function() {
    expect(action).toBeDefined();
  });
});
