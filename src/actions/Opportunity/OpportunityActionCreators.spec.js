'use strict';

describe('OpportunityActionCreators', function() {
  var action;

  beforeEach(function() {
    action = require('./OpportunityActionCreators.spec.js');
  });

  it('should be defined', function() {
    expect(action).toBeDefined();
  });
});
